<?xml version="1.0" encoding="UTF-8"?>
<extentreports>
    <configuration>
        <!-- report theme -->
        <!-- standard, dark -->
        <theme>standard</theme>
    
        <!-- viewstyle -->
        <!-- alt-view -->
        <viewstyle></viewstyle>

        <!-- document encoding -->
        <!-- defaults to UTF-8 -->
        <encoding>UTF-8</encoding>

		<!-- use thumbnails for base64 images -->
		<!-- this may slowdown viewing tests -->
		<thumbnailForBase64>true</thumbnailForBase64>

		<!-- offline report -->
        <enableOfflineMode>false</enableOfflineMode>
		
        <!-- enable or disable timeline on dashboard -->
        <enableTimeline>false</enableTimeline>

		<!-- enable or diisbale dark theme -->
		<theme>dark</theme>
        
        <!-- protocol for script and stylesheets -->
        <!-- defaults to https -->
        <protocol>https</protocol>
        
        <!-- title of the document -->
        <documentTitle>Google Maps UI Test Strategy</documentTitle>
        
        <!-- report name - displayed at top-nav -->
        <reportName>Google Maps Selenium C# NUnit Report</reportName>

		<!-- timestamp format -->
		<timeStampFormat>MMM dd, yyyy HH:mm:ss</timeStampFormat>
		
        <!-- custom javascript -->
        <scripts>
            <![CDATA[
                $(document).ready(function() {
                    
                });
            ]]>
        </scripts>
        
        <!-- custom styles -->
        <styles>
            <![CDATA[
                
            ]]>
        </styles>
    </configuration>
</extentreports>
